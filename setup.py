from setuptools import setup, find_packages


setup(author = 's3nh', name = 'img-cryptor', 
        packages = find_packages(), 
        descriptions = 'A simple library for dealing with image crypting.', 
        url = 'https://github.com/s3nh/img-cryptor', 
        author = 's3nh', 
        author_email = 'panek.damian@icloud.com', 
        license = 'WTFPL', 
        packages = find_packages(), 
        include_package_data = True, 
        zip_safe = False)